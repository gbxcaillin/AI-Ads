# Python side of the ai-commercial workflow. See scripts/setup.sh.
imageio-ffmpeg>=0.5     # bundled ffmpeg binary when ffmpeg is not on PATH
faster-whisper>=1.0     # word-timestamped transcription of the narration take (downloads the "small" model on first use)
